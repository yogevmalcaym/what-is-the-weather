import {css} from 'styled-components'
import {getColor} from 'styles/helper';

export const NoFavorites = css`
    font-size: 30px;
    color: ${getColor('background', 'dark ')};
    padding: 30px;
`;
